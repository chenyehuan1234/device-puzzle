# v0.6.0 · 2026-09-19 · 关卡库扩到 5 大关 / 72 关，并上线

## 1. 这次要解决什么

- 老板给了一份新导出的关卡库（`_245_deepseek\test\device-puzzle\关卡库.json`，17:45 导出），说「更新」。
- 目标：把这份新关卡库换进项目、打到单文件版里、发布到 GitHub Pages，线上和本地一致。
- **不做什么**：不改任何关卡数据、不动游戏规则、不动编辑器功能。

## 2. 改了什么

| 需求 | 结果 | 怎么验证的 |
| --- | --- | --- |
| 换入新关卡库 | `关卡库.json` 从 3 大关 / 45 关 → **5 大关 / 72 关**（8 + 15 + 23 + 22 + 4） | 换入后 `Get-FileHash` 与源文件 MD5 一致；`ConvertFrom-Json` 解析成功 |
| 版本号 | `MP.VERSION` `0.5.2` → `0.6.0` | 打包产物标题显示 `v0.6.0` |
| 根目录清理 | 两个旧库备份（45 关 / 46 关）移进 `backups\`，打包警告消失 | 重打包输出只剩「内置关卡包：关卡库.json → 5 个大关 / 72 关」，无 ⚠ |
| 线上同步 | `docs\index.html` 刷新为 v0.6.0 带关卡版（241.1 KB） | 文件大小比对 + 线上自检 |
| 加一个「关卡体检」工具 | 新增 `tools\check-levels.js`，一条命令列出所有数学上不可解的关 | 对当前库跑出 7 关问题，退出码 1 |

## 3. 涉及的文件

| 文件 | 改动 |
| --- | --- |
| `关卡库.json` | 整份替换为 5 大关 / 72 关的新库 |
| `js/core.js` | `MP.VERSION = '0.6.0'` |
| `docs/index.html` | 同步为新的带关卡单文件版 |
| `.gitignore` | 新增 `backups/`（旧库备份不再进仓库、也不干扰打包） |
| `backups/`（新目录，不入库） | 收拢 `关卡库-45关旧版.bak.json`、`关卡库.bak.json` |
| `tools/check-levels.js` | **新增**：关卡库体检工具（数量配平 / 空关 / 重名 / 重复关 / 孤儿关），有问题返回退出码 1 |
| `README.md` | 线上说明里的关卡数改成 5 大关 / 72 关；工具清单和发布流程里加上体检这一步 |
| `CHANGELOG.md` | 加 v0.6.0 一行 |

## 4. 规则 / 交互变化

- 游戏规则、编辑器交互**零变化**。这次纯粹是内容更新。
- 与前一份库的差异（自动比对结果）：
  - **新增 27 关**：第三章补 1 关（3-23），第四、五章整章新增（4-1 ~ 4-22、5-1 ~ 5-4）。
  - **改写 1 关**：`3-22` —— 上一版是个空关（0 图形 / 0 目标 / 0 设备），现在有 2 方 / 2 方目 + 1 圆 / 1 圆目 + 5 设备，**修好了**。
  - **删除 0 关**，无孤儿关卡、无悬空引用、无内容重复的关。

## 5. 测试

```
node tests/rules.test.js       通过 152 项，失败 0 项
node tests/bundle.test.js      通过  50 项，失败 0 项
node tests/ui.smoke.js         通过 196 项，失败 0 项
node tests/packaging.test.js   通过  64 项，失败 0 项
                               合计 462 项，失败 0
```

新增/修改的用例：无（本次没动代码逻辑，原有用例即可覆盖）。

新增工具（不是测试，但可当发布闸门）：

```
node tools/check-levels.js    → 列出 72 关的图形/目标配平情况，退出码 1（发现 7 关死局）
```

额外做的**关卡体检**（用 `MP.analyze()` 扫全部 72 关，脚本不改仓库文件）：

- 孤儿关卡：无；悬空引用：无；内容完全重复的关：无。
- ⚠ **7 关数量对不上，数学上不可解**（详见第 6 节）。

## 6. 已知问题 / 取舍

⚠ **以下 7 关「图形数 ≠ 同形状目标数」，通关条件（每个图形都停在同形状目标上）永远无法满足**，属于计数即可证明的死局，不需要跑搜索：

| 关卡 | 问题 |
| --- | --- |
| 1-5 | 正方形 1 个，方形目标 0 个 |
| 1-7 | 圆形 2 个，圆形目标 1 个 |
| 4-2 | 正方形 1 个，方形目标 0 个 |
| 4-5 | 正方形 2 个，方形目标 0 个 |
| 4-8 | 正方形 1 个，方形目标 0 个 |
| 4-17 | 圆形 1 个，圆形目标 0 个 |
| 5-2 | 正方形 3 个，方形目标 2 个 |

其中 **1-5、1-7 在上一版（45 关）里就已经是坏的**，这次没被修掉；而且它们在第一、四章前段，玩家很容易撞上。

> 处置：按项目惯例，**没有自动修改老板的关卡数据**。要修的话请定方案（补目标格 / 去掉多余图形 / 把图形换成另一种），我照做。

其他：

- 72 关里只有 2 关（2-8、4-22）带解法记录，其余 70 关没录。想批量确认「确实可解」，需要跑求解器，目前项目没做。
- `_245_deepseek\test\device-puzzle` 那份副本，**代码与主项目逐字节相同**（只有 README / CHANGELOG 是旧的），差异只在这份关卡库。以后建议只在主项目（`游戏开发\device-puzzle`）里改，避免两份分叉。

## 7. 下一步建议

1. 修掉上面 7 关（尤其是 1-5、1-7）。
2. 考虑加一个「关卡体检」按钮/脚本进项目，导出前先扫一遍数量是否配平。
3. 考虑给编辑器加「自动配平提示」：放下图形时若同形状目标不够，直接提示。
